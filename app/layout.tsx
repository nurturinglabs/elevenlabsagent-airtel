import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Airtel Voice AI Demo | ElevenLabs FDE Portfolio",
  description:
    "Enterprise voice AI customer support demo for Bharti Airtel, built with ElevenLabs Conversational AI. Portfolio project for FDE application.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased font-sans`}>
        {children}
      </body>
    </html>
  );
}
